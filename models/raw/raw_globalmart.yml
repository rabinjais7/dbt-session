version: 2

models:
  - name: raw_customers
    description: "this is description"
    columns:
      - name: customerid
        description: '{{ doc("customer_details")}}'
        tests:
          - not_null
          - unique
  - name: raw_orders
    columns:
      - name: orderid
        tests:
          - not_null
          - unique
        description: "Just testing1 "
      - name: shipmode
        tests:
          - accepted_values:
              values:
                - "Firstclass" 
                - "same days"

          
              
              
   
           
            
