version: 2

sources:
  - name: example_globalmart
    database: raw
    schema: globalmart
    tables:
      - name: order_example_1
        loaded_at_field: 'updated_at'
        freshness: 
          warn_after:
            count: 1
            period: hour
          error_after:
            count: 4
            period: hour
        